resources

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local deployment state — config, database, signing keys
fxa.toml
*.sqlite
*.sqlite-wal
*.sqlite-shm
signing-key.json
retired-key.json

# The edited copy of deploy/nginx.conf.example: it names your hostname and the
# paths to your certificates.
deploy/nginx.conf
