# What "the reference" means.
#
# fxa-lite is a reimplementation, not a fork: every protocol constant in plan.md and every
# "matches the reference" claim in the source was read out of a checkout under `resources/`,
# which is gitignored and untracked.  Without this file that provenance lives only on the
# machine that did the reading, and a claim that cannot name its source is not a claim.
#
# There are two kinds of entry, and the difference is what is being promised.
#
# `[[repo]]` is **tracked**: functionality fxa-lite replicates and has to stay current with.
# There are exactly two, and `tests/test_upstream.py` says so — `mozilla/fxa` and
# `mozilla-services/syncstorage-rs`.  These carry the paths that were actually read, not the
# repository, because `mozilla/fxa` is a monorepo whose churn is overwhelmingly
# subscriptions, Glean and the settings SPA, none of which we implement; the point of the
# path list is that `scripts/upstream-diff.sh` shows a protocol change as a handful of
# commits instead of two thousand irrelevant ones.  Bump a pin only together with the code
# change, or the note, that answers its diff.  The entry then always means "fxa-lite is
# current with respect to this commit", never "this is where we last happened to look".
#
# `[[reference]]` is **cited**: read once, to answer one question, and pinned so the answer
# can name a commit rather than a repository.  Most of these are the other side of the wire —
# what a client requires of the responses fxa-lite writes — and fxa-lite does not track them,
# so they carry no path list and `upstream-diff.sh` does not follow them.  The evidence a
# reference supports lives in `BUGS.md`, at the claim it supports, which is the only place a
# reader can check it.  A reference that grows a path list is a repository somebody promoted
# without arguing for it, and the tests refuse it.
#
# Every entry names its `license`.  That is not decoration: fxa-lite is MPL-2.0 because every
# project it took from is MPL-2.0, and the entries whose terms would have constrained harder
# — IronFox is AGPL-3.0, jwcrypto is LGPL-3.0, fxa-self-hosting has no LICENSE at all — are
# exactly the ones whose `took` line says "nothing".  See `docs/provenance.md`, *Licensing*.
#
# `tests/test_upstream.py` asserts every path below still exists in its checkout, and skips
# when `resources/` is absent.  Upstream renames files, and a stale path makes the diff *empty*
# — which reads as "nothing changed", the one failure mode this file cannot survive.  It also
# asserts that every checkout under `resources/` appears here as one kind or the other: a
# source nobody recorded is a source nobody can cite.

[[repo]]
dir = "fxa"
url = "https://github.com/mozilla/fxa"
branch = "main"
commit = "f87b36d0b92435869d2419993b1b5176b322b871"
date = "2026-08-24"
license = "MPL-2.0"
took = "the onepw protocol and its KATs, token derivation, the accounts/OAuth/profile wire format, and the WebChannel handshake"
paths = [
  # The client half of onepw — ported into tests/conformance/client.py — and its own test
  # vectors, transcribed into tests/vectors/.
  "packages/fxa-auth-client/lib",
  "packages/fxa-auth-client/test",
  # quickStretch, scrypt, verifyHash/wrapwrapKey, and the *.spec.ts files that pin them.
  "packages/fxa-auth-server/lib/crypto",
  # tokenTypeID derivation and the single-use keyFetchToken bundle, likewise with their specs.
  "packages/fxa-auth-server/lib/tokens",
  # grant.js, jwt_access_token.js (incl. the oldsync `aud` swap), keys.ts, validators.js.
  "packages/fxa-auth-server/lib/oauth",
  # hawk-fxa-token.js: the auth scheme we match, MAC-discarding included.
  "packages/fxa-auth-server/lib/routes/auth-schemes",
  # The routes fxa-lite serves, and the shared response furniture around them.
  "packages/fxa-auth-server/lib/routes/account.ts",
  "packages/fxa-auth-server/lib/routes/session.js",
  "packages/fxa-auth-server/lib/routes/devices-and-sessions.js",
  # Its spec is the citation for what /account/devices/notify answers when the
  # deployment does not do device-driven notifications: 403, errno 202.
  "packages/fxa-auth-server/lib/routes/devices-and-sessions.spec.ts",
  # The command queue itself, read twice and for opposite conclusions.  First
  # for what every method does when the deployment has no pushbox (`enabled =
  # false` — reject with featureNotEnabled), which is where the 403 on
  # /account/device/commands came from; then, after that 403 turned out to
  # crash Firefox for Android, for `PushboxDB.retrieve`, which computes the
  # empty-queue document that replaced it — `index` 0, `last` true, no messages.
  "packages/fxa-auth-server/lib/pushbox",
  "packages/fxa-auth-server/lib/routes/emails.js",
  "packages/fxa-auth-server/lib/routes/oauth",
  # attached_clients, plus the spec whose fixture is transcribed as its KAT.
  "packages/fxa-auth-server/lib/routes/attached-clients.js",
  "packages/fxa-auth-server/lib/routes/attached-clients.spec.ts",
  "packages/fxa-auth-server/lib/routes/defaults.js",
  "packages/fxa-auth-server/lib/routes/util.js",
  "packages/fxa-auth-server/lib/routes/validators.js",
  "packages/fxa-auth-server/lib/devices.js",
  # Where the three browser client ids, scopes and redirect URIs come from.
  "packages/fxa-auth-server/config/dev.json",
  # Read narrowly, for what a switch means rather than for its value —
  # deviceNotificationsEnabled is documented there as a temporary safety switch,
  # which is why fxa-lite's permanent version of it answers differently.  One
  # value *is* taken from it: oauth.deviceManagementClientIds, copied verbatim
  # into oauth/clients.py, because that default is the authorization rule the
  # refresh-token auth scheme applies and not a deployment preference.
  "packages/fxa-auth-server/config/index.ts",
  # The errno table behind errors.py.
  "libs/accounts/errors/src",
  # Scoped keys, the oldsync special case, and the keys_jwe bundle — plus their specs.
  "libs/vendored/crypto-relier/src/lib/deriver",
  # ScopeSet, ported to oauth/scopes.py, and the KATs it is checked against.
  "packages/fxa-shared/oauth/scopes.ts",
  "packages/fxa-shared/test/oauth/scopes.js",
  # ConnectedServicesFactory: the merge behind /account/attached_clients.
  "packages/fxa-shared/connected-services",
  # The profile server: a second errno table and the /v1/{profile,email,uid,display_name} set,
  # plus the avatar routes — `routes/profile.js:nextAvatar` and `routes/avatar/default.js` are
  # where the monogram URL and its SVG come from.
  "packages/fxa-profile-server/lib/error.js",
  "packages/fxa-profile-server/lib/routes",
  # Where `/v1/avatar/{id}` is mounted, and public.
  "packages/fxa-profile-server/lib/routing.js",
  # Why `avatar` and `avatarDefault` are both present or both absent: the profile document is
  # assembled from four internal calls, and a scope-forbidden one drops its fields as a pair.
  "packages/fxa-profile-server/lib/batch.js",
  "packages/fxa-profile-server/lib/profileCache.js",
  # WebChannel: command names and envelope, and two working emitters to copy structure from.
  "packages/fxa-settings/src/lib/channels/firefox.ts",
  "packages/fxa-settings/src/pages/Signin/utils.ts",
  "packages/fxa-settings/src/pages/WebChannelExample/index.tsx",
  "packages/functional-tests/pages/layout.ts",
  # fx-sync-channel.js — the fields the browser side requires of a login message.
  "packages/fxa-content-server/app/scripts/models/auth_brokers",
  # The discovery document Firefox reads before anything else.
  "packages/fxa-content-server/server/lib/routes/get-fxa-client-configuration.js",
  # Read to answer "can a client toggle open a path we do not serve": no.
  # `index.js` groups the *same* route names under feature flags and
  # `add-routes.js` registers React and Backbone on each of them, choosing
  # between the two on `?showReactApp=true`.  The lever is a query parameter.
  "packages/fxa-content-server/server/lib/routes/react-app",
  # The about:config prefs phase 8 sets on a fresh profile.
  "packages/fxa-dev-launcher/profile.mjs",
]

[[repo]]
dir = "syncstorage-rs"
url = "https://github.com/mozilla-services/syncstorage-rs"
branch = "master"
commit = "3f0f985cc92a72647a3a701da702a3d5f28d05df"
date = "2026-08-21"
license = "MPL-2.0"
took = "tokenlib, the tokenserver exchange, Sync 1.5 storage semantics, and the HAWK verification we do run"
paths = [
  # tokenlib: signing key, payload, token, derived secret.
  "tokenserver-auth/src/token/native.rs",
  "tokenserver-auth/src/crypto.rs",
  # /1.0/sync/1.5 — extraction, the response body, and the tokenserver's error envelope.
  "syncserver/src/tokenserver",
  "tokenserver-common/src/error.rs",
  # HAWK, whose module tests carry the worked example syncstorage/hawk.py is pinned against.
  "syncserver/src/web/auth.rs",
  # Query parameters, precondition headers, batch requests, and the endpoint set over them.
  "syncserver/src/web/extractors",
  "syncserver/src/web/handlers.rs",
  # The bare-JSON-integer Weave error body, deliberate rather than an oversight.
  "syncserver/src/error.rs",
  # BSO and batch semantics, including the two upstream bugs we chose not to reproduce.
  "syncstorage-mysql/src/db",
  "syncstorage-db-common/src",
  # The /info/configuration limits, fixed here rather than configurable.
  "syncstorage-settings/src/lib.rs",
]

# ---------------------------------------------------------------------------------------
# Read once, pinned for citation.  Not tracked, not diffed, no path lists — see the header.
# ---------------------------------------------------------------------------------------

# The client side of the wire.  Every Firefox for Android build embeds this crate, so what
# it will and will not deserialise is a hard constraint on what fxa-lite may write, and
# three household bug reports (phases 13, 14, 16) were closed by reading it rather than by
# taking a trace.  The specific files and symbols are cited in BUGS.md at AS-1..AS-3 and in
# docs/clients.md, which is where a reader can check them against this pin.
[[reference]]
dir = "application-services"
url = "https://github.com/mozilla/application-services"
branch = "main"
commit = "7674e0cf977272c746eebee7c145c982e06cc6c4"
date = "2026-08-25"
license = "MPL-2.0"
took = "what the mobile client requires of fxa-lite's responses: that `avatar` is present in a profile document at all, that a 403 is read as a status and never as an errno, which content-server paths it builds from a literal rather than from discovery, how it fixes up a tokenserver URL, and the exact shape of a mobile collection download — which query it carries, when it decides not to send one, and where its high-water mark comes from"

# Fenix and android-components, archived in June 2024 — both now live in the Firefox
# monorepo, so `upstream-diff.sh` would report nothing for this pin forever even if it
# followed it.  Kept because the alternative is a claim about Kotlin with no checkout behind
# it, and because the two files that carry the argument have held the same allow-list since
# long before the archive.  Anything load bearing read from here wants re-checking against
# mozilla-firefox/firefox below; BUGS.md FA-1..FA-3 say which files those are.
[[reference]]
dir = "firefox-android"
url = "https://github.com/mozilla-mobile/firefox-android"
branch = "main"
commit = "fe8a71cd70ad5674abe1824fe11dc78372b736c2"
date = "2024-06-17"
license = "MPL-2.0"
took = "the Kotlin half of a force-close — which FxA errors android-components treats as recoverable, which line of Fenix polls the route that raised one, and the coroutine it is rethrown into with no handler — plus the early return that renders a signed-in account as 'Sign in', and where the Sync Debug preferences are read and where a restored account stops reading them"

# A full mozilla-central checkout.  Cloned in phase 16 to read Desktop's Sync engines, and
# it answered one question with a negative: the Rust crates the phone runs are not vendored
# here, so the mobile side of that investigation is `application-services` above.  Phase 17
# gave it a second job and a real one — the browser half of BUGS.md FXA-1 was originally
# read at gecko-dev HEAD, unpinned, which plan.md phase 8 recorded as a provenance gap.
# `services/common/hawkclient.sys.mjs`, `services/fxaccounts/FxAccountsClient.sys.mjs` and
# `services/sync/modules/engines/clients.sys.mjs` are all here, so that gap is closed and
# the claim now cites this commit.
[[reference]]
dir = "firefox"
url = "https://github.com/mozilla-firefox/firefox"
branch = "main"
commit = "39532fe5a7a63cf58953e0ce2e2e264c94313b12"
date = "2026-08-26"
license = "MPL-2.0"
took = "how Firefox Desktop's account client handles a `Retry-After` — the backoff it caches on any status and applies to every subsequent FxA request, which is what makes a permanently-disabled feature stall the whole client; and Desktop's Sync engines for contrast with the Rust ones, which took nothing but is the reason the tree was opened"

# The fork the household's phone actually runs.  AGPL-3.0, and nothing is taken from it —
# which is not pedantry: see the header, and docs/provenance.md, *Licensing*.  Its patch set
# is the cheapest available proof of which Fenix file decides what, because it patches by
# path against the Firefox monorepo.
[[reference]]
dir = "IronFox"
url = "https://gitlab.com/ironfox-oss/IronFox"
branch = "dev"
commit = "b1bd8fa03466a5555ece638476722c6a36897640"
date = "2026-08-25"
license = "AGPL-3.0"
took = "nothing — read only as a path index into Fenix, where its avatar patch names the menu item and the settings view that each decide between an address and 'Sign in'"

# What self-hosting the real thing costs, which is the project's whole premise, and the
# explicit per-service client prefs for when autoconfig misbehaves.
[[reference]]
dir = "fxa-selfhosting"
url = "https://github.com/jackyzy823/fxa-selfhosting"
branch = "master"
commit = "200626f1e59243536cfa9518e2168ac21b9e2bed"
date = "2026-08-21"
license = "MPL-2.0"
took = "the shape of a real deployment — MySQL, eight Redis hostnames, Firestore, an SMTP relay, nginx and a channelserver — and the explicit `identity.fxaccounts.*` prefs, plus both spellings of Fenix's two Sync Debug fields"

# No LICENSE file, so all rights reserved by default.  Nothing is taken; only the README was
# ever opened.  Kept to show how long the problem has been unsolved.
[[reference]]
dir = "fxa-self-hosting"
url = "https://github.com/michielbdejong/fxa-self-hosting"
branch = "master"
commit = "2343760c34197de7d7e6ec71118ce027a0e76e5a"
date = "2016-05-13"
license = "none — no LICENSE file, all rights reserved"
took = "nothing — historical only, ten years stale"

# The three JOSE libraries, evaluated and none adopted.  Each is pinned at the release that
# was read rather than at head: a newer commit answers no question fxa-lite is asking, so
# these entries are informational until somebody reopens the question.  python-jose was
# rejected in phase 0 (it cannot do ECDH-ES at all); joserfc and jwcrypto both answered
# phase 9's two questions yes and were still not adopted, because nothing in `src/` decrypts
# a JWE and the hostile-input surface a library would take over is about ninety lines.
[[reference]]
dir = "python-jose"
url = "https://github.com/mpdavis/python-jose"
branch = "master"
commit = "018b310ddb8b50dcfd09a0c152117835a21dd656"
date = "2025-05-28"
license = "MIT"
took = "nothing — evaluated at 3.5.0: ECDH-ES is in ALL but not SUPPORTED and both directions gate on SUPPORTED, its default backend is pure-Python crypto costing three dependencies, and 3.5.0 is the last release"

[[reference]]
dir = "joserfc"
url = "https://github.com/authlib/joserfc"
branch = "main"
commit = "7facdee77fc8735ad88e586b31c5b0c940ee0a78"
date = "2026-07-13"
license = "BSD-3-Clause"
took = "nothing — evaluated at 1.7.4: it answers both of phase 9's questions yes, cross-decrypts against tests/vectors/jose.json unchanged, and costs exactly one dependency, and was still not adopted"

[[reference]]
dir = "jwcrypto"
url = "https://github.com/latchset/jwcrypto"
branch = "main"
commit = "eb7c266d18d1a45abce420492d48a3d01d281916"
date = "2026-06-24"
license = "LGPL-3.0"
took = "nothing — evaluated at 1.5.8 alongside joserfc, same outcome; and LGPL-3.0, which would have been the first term in this file to constrain fxa-lite's own licence had it been adopted"
