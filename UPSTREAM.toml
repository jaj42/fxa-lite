# What "the reference" means.
#
# fxa-lite is a reimplementation, not a fork: every protocol constant in plan.md and every
# "matches the reference" claim in the source was read out of a checkout under `resources/`,
# which is gitignored and untracked.  Without this file that provenance lives only on the
# machine that did the reading, and a claim that cannot name its source is not a claim.
#
# Each entry pins one checkout to the commit fxa-lite was read against, and lists the paths
# that were actually read — not the repository.  mozilla/fxa is a monorepo whose churn is
# overwhelmingly subscriptions, Glean and the settings SPA, none of which we implement; the
# point of the path list is that `scripts/upstream-diff.sh` shows a protocol change as a
# handful of commits instead of two thousand irrelevant ones.
#
# Bump a pin only together with the code change, or the note, that answers its diff.  The file
# then always means "fxa-lite is current with respect to this commit", never "this is where we
# last happened to look".
#
# `tests/test_upstream.py` asserts every path below still exists in its checkout, and skips
# when `resources/` is absent.  Upstream renames files, and a stale path makes the diff *empty*
# — which reads as "nothing changed", the one failure mode this file cannot survive.

[[repo]]
dir = "fxa"
url = "https://github.com/mozilla/fxa"
branch = "main"
commit = "f87b36d0b92435869d2419993b1b5176b322b871"
date = "2026-08-24"
took = "the onepw protocol and its KATs, token derivation, the accounts/OAuth/profile wire format, and the WebChannel handshake"
paths = [
  # The client half of onepw — ported into tests/conformance/client.py — and its own test
  # vectors, transcribed into tests/vectors/.
  "packages/fxa-auth-client/lib",
  "packages/fxa-auth-client/test",
  # quickStretch, scrypt, verifyHash/wrapwrapKey, and the *.spec.ts files that pin them.
  "packages/fxa-auth-server/lib/crypto",
  # tokenTypeID derivation and the single-use keyFetchToken bundle, likewise with their specs.
  "packages/fxa-auth-server/lib/tokens",
  # grant.js, jwt_access_token.js (incl. the oldsync `aud` swap), keys.ts, validators.js.
  "packages/fxa-auth-server/lib/oauth",
  # hawk-fxa-token.js: the auth scheme we match, MAC-discarding included.
  "packages/fxa-auth-server/lib/routes/auth-schemes",
  # The routes fxa-lite serves, and the shared response furniture around them.
  "packages/fxa-auth-server/lib/routes/account.ts",
  "packages/fxa-auth-server/lib/routes/session.js",
  "packages/fxa-auth-server/lib/routes/devices-and-sessions.js",
  # Its spec is the citation for what /account/devices/notify answers when the
  # deployment does not do device-driven notifications: 403, errno 202.
  "packages/fxa-auth-server/lib/routes/devices-and-sessions.spec.ts",
  # The command queue itself, read twice and for opposite conclusions.  First
  # for what every method does when the deployment has no pushbox (`enabled =
  # false` — reject with featureNotEnabled), which is where the 403 on
  # /account/device/commands came from; then, after that 403 turned out to
  # crash Firefox for Android, for `PushboxDB.retrieve`, which computes the
  # empty-queue document that replaced it — `index` 0, `last` true, no messages.
  "packages/fxa-auth-server/lib/pushbox",
  "packages/fxa-auth-server/lib/routes/emails.js",
  "packages/fxa-auth-server/lib/routes/oauth",
  # attached_clients, plus the spec whose fixture is transcribed as its KAT.
  "packages/fxa-auth-server/lib/routes/attached-clients.js",
  "packages/fxa-auth-server/lib/routes/attached-clients.spec.ts",
  "packages/fxa-auth-server/lib/routes/defaults.js",
  "packages/fxa-auth-server/lib/routes/util.js",
  "packages/fxa-auth-server/lib/routes/validators.js",
  "packages/fxa-auth-server/lib/devices.js",
  # Where the three browser client ids, scopes and redirect URIs come from.
  "packages/fxa-auth-server/config/dev.json",
  # Read narrowly, for what a switch means rather than for its value —
  # deviceNotificationsEnabled is documented there as a temporary safety switch,
  # which is why fxa-lite's permanent version of it answers differently.  One
  # value *is* taken from it: oauth.deviceManagementClientIds, copied verbatim
  # into oauth/clients.py, because that default is the authorization rule the
  # refresh-token auth scheme applies and not a deployment preference.
  "packages/fxa-auth-server/config/index.ts",
  # The errno table behind errors.py.
  "libs/accounts/errors/src",
  # Scoped keys, the oldsync special case, and the keys_jwe bundle — plus their specs.
  "libs/vendored/crypto-relier/src/lib/deriver",
  # ScopeSet, ported to oauth/scopes.py, and the KATs it is checked against.
  "packages/fxa-shared/oauth/scopes.ts",
  "packages/fxa-shared/test/oauth/scopes.js",
  # ConnectedServicesFactory: the merge behind /account/attached_clients.
  "packages/fxa-shared/connected-services",
  # The profile server: a second errno table and the /v1/{profile,email,uid,display_name} set,
  # plus the avatar routes — `routes/profile.js:nextAvatar` and `routes/avatar/default.js` are
  # where the monogram URL and its SVG come from.
  "packages/fxa-profile-server/lib/error.js",
  "packages/fxa-profile-server/lib/routes",
  # Where `/v1/avatar/{id}` is mounted, and public.
  "packages/fxa-profile-server/lib/routing.js",
  # Why `avatar` and `avatarDefault` are both present or both absent: the profile document is
  # assembled from four internal calls, and a scope-forbidden one drops its fields as a pair.
  "packages/fxa-profile-server/lib/batch.js",
  "packages/fxa-profile-server/lib/profileCache.js",
  # WebChannel: command names and envelope, and two working emitters to copy structure from.
  "packages/fxa-settings/src/lib/channels/firefox.ts",
  "packages/fxa-settings/src/pages/Signin/utils.ts",
  "packages/fxa-settings/src/pages/WebChannelExample/index.tsx",
  "packages/functional-tests/pages/layout.ts",
  # fx-sync-channel.js — the fields the browser side requires of a login message.
  "packages/fxa-content-server/app/scripts/models/auth_brokers",
  # The discovery document Firefox reads before anything else.
  "packages/fxa-content-server/server/lib/routes/get-fxa-client-configuration.js",
  # Read to answer "can a client toggle open a path we do not serve": no.
  # `index.js` groups the *same* route names under feature flags and
  # `add-routes.js` registers React and Backbone on each of them, choosing
  # between the two on `?showReactApp=true`.  The lever is a query parameter.
  "packages/fxa-content-server/server/lib/routes/react-app",
  # The about:config prefs phase 8 sets on a fresh profile.
  "packages/fxa-dev-launcher/profile.mjs",
]

[[repo]]
dir = "syncstorage-rs"
url = "https://github.com/mozilla-services/syncstorage-rs"
branch = "master"
commit = "3f0f985cc92a72647a3a701da702a3d5f28d05df"
date = "2026-08-21"
took = "tokenlib, the tokenserver exchange, Sync 1.5 storage semantics, and the HAWK verification we do run"
paths = [
  # tokenlib: signing key, payload, token, derived secret.
  "tokenserver-auth/src/token/native.rs",
  "tokenserver-auth/src/crypto.rs",
  # /1.0/sync/1.5 — extraction, the response body, and the tokenserver's error envelope.
  "syncserver/src/tokenserver",
  "tokenserver-common/src/error.rs",
  # HAWK, whose module tests carry the worked example syncstorage/hawk.py is pinned against.
  "syncserver/src/web/auth.rs",
  # Query parameters, precondition headers, batch requests, and the endpoint set over them.
  "syncserver/src/web/extractors",
  "syncserver/src/web/handlers.rs",
  # The bare-JSON-integer Weave error body, deliberate rather than an oversight.
  "syncserver/src/error.rs",
  # BSO and batch semantics, including the two upstream bugs we chose not to reproduce.
  "syncstorage-mysql/src/db",
  "syncstorage-db-common/src",
  # The /info/configuration limits, fixed here rather than configurable.
  "syncstorage-settings/src/lib.rs",
]

[[repo]]
dir = "fxa-selfhosting"
url = "https://github.com/jackyzy823/fxa-selfhosting"
branch = "master"
commit = "200626f1e59243536cfa9518e2168ac21b9e2bed"
date = "2026-08-21"
took = "what self-hosting the real thing costs, and the explicit client prefs for when autoconfig misbehaves"
paths = [
  "docker-compose.tmpl.yml",
  "config.yml.sample",
  "init.sh",
]

[[repo]]
dir = "fxa-self-hosting"
url = "https://github.com/michielbdejong/fxa-self-hosting"
branch = "master"
commit = "2343760c34197de7d7e6ec71118ce027a0e76e5a"
date = "2016-05-13"
took = "nothing — historical only, ten years stale, kept to show how long the problem has been unsolved"
paths = [
  "README.md",
]

[[repo]]
dir = "python-jose"
url = "https://github.com/mpdavis/python-jose"
branch = "master"
commit = "018b310ddb8b50dcfd09a0c152117835a21dd656"
date = "2025-05-28"
took = "nothing — evaluated at 3.5.0 and rejected; see plan.md, Decisions already made"
paths = [
  # ECDH-ES is in ALL but not SUPPORTED, and both directions gate on SUPPORTED.
  "jose/jwe.py",
  "jose/jws.py",
  "jose/jwt.py",
  # The pure-Python default backend, and the three dependencies that come with it.
  "jose/backends",
  "setup.py",
  # 3.4.0 fixed CVE-2024-33663/33664; 3.5.0 is the last release.
  "CHANGELOG.md",
]

[[repo]]
dir = "joserfc"
url = "https://github.com/authlib/joserfc"
branch = "main"
commit = "7facdee77fc8735ad88e586b31c5b0c940ee0a78"
date = "2026-07-13"
took = "nothing — evaluated at 1.7.4 in phase 9: it answers both questions yes and was still not adopted, see plan.md. Pinned at the release that was evaluated, not at head; a newer commit here answers no question fxa-lite is asking, so this entry's log is informational until somebody re-opens the question"
paths = [
  # RS256 and compact ECDH-ES+A256GCM both work against tests/vectors/jose.json
  # unchanged, and `algorithms=` is a per-call argument on both.
  "src/joserfc/jws.py",
  "src/joserfc/jwe.py",
  "src/joserfc/jwt.py",
  # The concat KDF and the ECDH-ES agreement, which is the half python-jose could not do.
  "src/joserfc/_rfc7518/derive_key.py",
  "src/joserfc/_rfc7518/jwe_algs.py",
  # Header validation: where the 1 KiB header cap and the `crit` handling live.
  "src/joserfc/registry.py",
  "src/joserfc/errors.py",
  # One dependency, and it is `cryptography` — the python-jose objection does not apply.
  "pyproject.toml",
]

[[repo]]
dir = "jwcrypto"
url = "https://github.com/latchset/jwcrypto"
branch = "main"
commit = "eb7c266d18d1a45abce420492d48a3d01d281916"
date = "2026-06-24"
took = "nothing — evaluated at 1.5.8 in phase 9 alongside joserfc, same outcome, and pinned at the evaluated release for the same reason"
paths = [
  "jwcrypto/jws.py",
  "jwcrypto/jwe.py",
  "jwcrypto/jwt.py",
  # The registry of algorithms a token is allowed to name, and the default allow-list —
  # the thing an `algorithms=` argument is compared against.
  "jwcrypto/jwa.py",
  "pyproject.toml",
]

[[repo]]
dir = "application-services"
url = "https://github.com/mozilla/application-services"
branch = "main"
commit = "7674e0cf977272c746eebee7c145c982e06cc6c4"
date = "2026-08-25"
took = "what Firefox for Android requires of the responses fxa-lite writes — the fxa-client Rust crate that every mobile build embeds, read after a phone that synced fine still offered to sign in; then, in phase 15, what that crate does with an empty Sync Debug field, which content-server paths it builds from a literal, and how sync15 fixes up a tokenserver URL; and in phase 16 the shape of a mobile *download* — which query a collection read actually carries, when the client decides not to send it at all, and where its high-water mark comes from"
paths = [
  # `ProfileResponse`: `avatar` is a plain `String`, so a profile document without
  # the key fails to parse and the phone never learns its own address.  Also the
  # home of every other response struct the mobile client deserialises.
  "components/fxa-client/src/internal/http_client.rs",
  # `get_profile` and its ETag cache — the caller of the above.
  "components/fxa-client/src/internal/profile.rs",
  # `userinfo_endpoint` comes from `.well-known/openid-configuration`, not from
  # `fxa-client-configuration`; `normalize_token_server_url` strips a
  # `/1.0/sync/1.5` suffix from the override, and `token_server_endpoint_url`
  # falls back to `sync_tokenserver_base_url` when there is no override — half
  # of phase 15's answer, the other half being sync15's `fixup_server_url`.
  # It is also where the content-server paths the client builds from a literal
  # live: `oauth/force_auth`, `pair`, `pair/supp`, `connect_another_device`.
  "components/fxa-client/src/internal/config.rs",
  # `begin_oauth_flow`: `authorization_endpoint` from discovery normally, and
  # `oauth_force_auth_url()` — a literal, not a discovered value — whenever the
  # account has a profile but no session token.  A 404 there is a phone that
  # cannot re-authenticate, which is why `PAGE_PATHS` serves it.
  "components/fxa-client/src/internal/oauth.rs",
  # `StateV2.config`: the persisted account carries the server it signed in
  # against, so `from_json` restores that and not the app's current settings.
  "components/fxa-client/src/internal/state_persistence.rs",
  # Where a failed `get_profile` lands the account: `AuthIssues`, not a retry.
  "components/fxa-client/src/state_machine",
  # `fixup_server_url`, and the unit test that pins it: the tokenserver URL is
  # given `/1.0/sync/1.5` if it does not already end in it, whether it came
  # from discovery or from a self-hoster's override.  That is why Fenix's
  # *Custom Sync server* field is not needed — the answer to the first of
  # phase 8's four questions, closed in phase 15.
  "components/sync15/src/client/token.rs",
  # `build_collection_request_url`: the whole vocabulary of a mobile collection
  # read — `full=1` as a value, `ids` comma-joined, `older`, `newer`, and
  # `sort` only ever alongside `limit`.  Also `Sync15ClientResponse::from_response`,
  # where a 2xx without a parseable `X-Last-Modified` is a hard error, which is
  # what makes that header load bearing on every storage route we serve.
  "components/sync15/src/client/storage_client.rs",
  # `synchronize_with_clients_engine`: download, then apply, then upload, in
  # that order and within one engine.  That ordering is the deduction that
  # closed phase 16 — a phone whose records reach the desktop has, by
  # construction, already run its own download.  Also the two silent exits: a
  # `get_collection_request` of `None` and a collection state of `None` both
  # return `Ok(())` having fetched nothing.
  "components/sync15/src/client/sync.rs",
  # `LocalCollStateMachine`: `CollState.last_modified` is the *`info/collections`*
  # value, not the engine's own mark and not the collection GET's header.
  "components/sync15/src/client/coll_state.rs",
  # `fetch_incoming` binds the GET's `X-Last-Modified` to `_timestamp` and drops
  # it, which is why a record written between a phone's read and its own write
  # is skipped for good.
  "components/sync15/src/client/coll_update.rs",
  # `CollectionRequest`/`RequestOrder`, and the comment that explains why `sort`
  # travels with `limit`: "asking for the order of records only makes sense if
  # you are limiting them".  There is no `offset` field, so the client cannot
  # page — see the note at `get_bsos` in `syncstorage/store.py`.
  "components/sync15/src/engine/request.rs",
  # `Display for ServerTimestamp` is `self.0 as f64 / 1000.0`, so `newer=` is
  # the shortest round-tripping form and not the two decimals `X-Last-Modified`
  # always carries.  One instant, two spellings, both on the wire.
  "components/sync15/src/server_timestamp.rs",
  # `get_collection_request`: `since == server_timestamp` means the request is
  # never made, and `LAST_SYNC_META_KEY` is written in `apply()` from that same
  # `info/collections` value.  The bookmarks engine sends `full()` and
  # `newer_than(since)` and nothing else.
  "components/places/src/bookmark_sync/engine.rs",
]

# A full mozilla-central checkout, cloned while chasing a report that desktop
# records were not reaching a phone.  It answered one question and it was a
# negative: the Rust Sync client Fenix runs is *not* vendored here — there is no
# `sync15` or `places` under `third_party/rust` — so the mobile side of that
# investigation is `application-services` above, and Fenix's Kotlin is
# `firefox-android` below.  Nothing in fxa-lite rests on this tree; it is pinned
# because a checkout under `resources/` that the manifest does not describe is
# exactly the provenance hole phase 7 exists to close, and "read and took
# nothing" is a recorded answer where silence is not.
[[repo]]
dir = "firefox"
url = "https://github.com/mozilla-firefox/firefox"
branch = "main"
commit = "39532fe5a7a63cf58953e0ce2e2e264c94313b12"
date = "2026-08-26"
took = "nothing — cloned in phase 16 to read Desktop's Sync engines and to check whether the Rust crates the phone runs are vendored alongside them. They are not, and Desktop was never the failing half; the entry records the look rather than a dependency"
paths = [
  # Desktop's Sync client, for contrast with the Rust one: this is where the
  # collection reads that *do* carry `sort` and `limit` are built, and it is the
  # half of the ecosystem fxa-lite had already been tested against.  Listed as
  # the tree that was actually opened, so a rename shows up in the diff even
  # though no constant was taken from it.
  "services/sync/modules",
]

[[repo]]
dir = "IronFox"
url = "https://gitlab.com/ironfox-oss/IronFox"
branch = "dev"
commit = "b1bd8fa03466a5555ece638476722c6a36897640"
date = "2026-08-25"
took = "nothing — the fork the household's phone actually runs, kept because its patch set is the cheapest proof of which Fenix file does what: it patches by path against the Firefox monorepo"
paths = [
  # Names `components/menu/compose/header/MozillaAccountMenuItem.kt` as the menu
  # item that renders either the address or "Sign in", and `AccountUiView.kt` as
  # the settings screen's version of the same decision.
  "patches/fenix-disable-sync-avatar-fetching.patch",
]

# Archived in June 2024: Fenix and android-components now live in the Firefox
# monorepo, so this pin is the last state of a tree the phone no longer builds
# from.  It is kept anyway, and its staleness stated rather than hidden, because
# the two files that matter here — `Exceptions.kt` and `Utils.kt` — have carried
# the same allow-list since long before the archive, and `scripts/upstream-diff.sh`
# will report nothing for this repo forever.  Anything read from it that is load
# bearing wants confirming against a handset or against mozilla-central; the
# force-close it explains is confirmed by the phone having stopped crashing.
[[repo]]
dir = "firefox-android"
url = "https://github.com/mozilla-mobile/firefox-android"
branch = "main"
commit = "fe8a71cd70ad5674abe1824fe11dc78372b736c2"
date = "2024-06-17"
took = "the Kotlin half of the answer to a force-close: which FxA errors android-components treats as recoverable, and which line of Fenix polls the route that raised one; and in phase 15 the Kotlin half of the Sync Debug fields — where the two preferences are read, and where a restored account stops reading them"
paths = [
  # `shouldPropagate()`: the allow-list of recoverable `FxaException`s, ending
  # `else -> true`.  `Forbidden` is not on it and has no typealias here, which
  # is why a 403 from /account/device/commands crashes the app rather than
  # being swallowed the way the earlier 404 was.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/Exceptions.kt",
  # `handleFxaExceptions`, which asks `shouldPropagate()` before it decides
  # anything — so the `default`/`handleErrorBlock` a caller passes never runs.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/Utils.kt",
  # `pollForCommands()`, the caller, and `refreshDevices()` next to it: the
  # /account/devices then /account/device/commands pair seen in the server log.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/FxaDeviceConstellation.kt",
  # `authenticatedAccount()` returns the account for AuthenticationProblem as
  # well as Authenticated — which is why the crash was not introduced by the
  # profile fix, only made easy to reach.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/manager/FxaAccountManager.kt",
  # `SyncStoreSupport`: the early return that rendered a missing profile as
  # "Sign in", which phase 13 read from a copy rather than from a checkout.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/store/SyncStoreSupport.kt",
  # `syncNow()`: `refreshDevices()` then `pollForCommands()`, inside
  # `viewLifecycleOwner.lifecycleScope.launch` — the coroutine with no handler
  # that the rethrown exception escapes from.  This is the *Sync now* button.
  "fenix/app/src/main/java/org/mozilla/fenix/settings/account/AccountSettingsFragment.kt",
  # The three files behind phase 15's answers about the Sync Debug fields.
  # `FxaServer.config()` builds the `ServerConfig` from the two preferences and
  # passes `overrideSyncTokenServer.ifEmpty { null }`, so an empty field is no
  # override at all and discovery decides.
  "fenix/app/src/main/java/org/mozilla/fenix/components/FxaServer.kt",
  # `SyncDebugFragment`: editing either field reveals the *Quit application*
  # preference, which is `exitProcess(0)`.  The app has to restart; it offers.
  "fenix/app/src/main/java/org/mozilla/fenix/settings/SyncDebugFragment.kt",
  # `StorageWrapper.account()`: saved state is `AccountOnDisk.Restored`, rebuilt
  # from JSON whose persisted config carries its own server — which is why the
  # preferences do nothing to an account that is already signed in.
  "android-components/components/service/firefox-accounts/src/main/java/mozilla/components/service/fxa/AccountStorage.kt",
]
